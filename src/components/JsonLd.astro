---
// Renders a single JSON-LD block. Pass any schema.org object as `schema`.
interface Props {
  schema: Record<string, unknown>;
}
const { schema } = Astro.props;
---
<script
  type="application/ld+json"
  set:html={JSON.stringify(schema)}
/>
